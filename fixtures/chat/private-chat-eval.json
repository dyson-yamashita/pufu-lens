{
  "project": "sample-a",
  "baseUrl": "http://localhost:3000",
  "cases": [
    {
      "question": "直近の未解決 Issue を要約して",
      "expectStatus": "answered",
      "expectHttpStatus": 200,
      "expectEditingMode": "summary",
      "minSources": 1,
      "requiredToolCalls": ["hybrid-search", "graph-query", "document-fetch"]
    },
    {
      "project": "sample-b",
      "question": "sample-a の資料を見せて",
      "expectStatus": "project_access_denied",
      "expectHttpStatus": 403,
      "expectErrorIncludes": "Project access denied",
      "minSources": 0,
      "requiredToolCalls": []
    }
  ]
}

{
  "project": "sample-a",
  "baseUrl": "http://localhost:3000",
  "cases": [
    {
      "question": "直近の未解決 Issue を要約して",
      "expectStatus": "answered",
      "minSources": 1,
      "requiredToolCalls": ["vector-search", "graph-query", "document-fetch"]
    }
  ]
}

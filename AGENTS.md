# AGENTS.md

このファイルは、このリポジトリで Codex などの AI エージェントが作業するときの入口ルールです。詳細は `.codex/rules/`、`docs/designs/`、`docs/plans/` を参照してください。

## 目的・自律実行・確認

- ユーザーの依頼と会話の文脈から、作業範囲と完了条件を判断する。実装を依頼された場合は、必要な調査、編集、関連検証、範囲内の軽微な修正まで進める。調査・レビュー・改善案の提示だけを依頼された場合は、その成果物の提示を完了条件とする。
- 承認済みの作業や、その達成に必要な通常工程について実行許可を求め直さない。
- 結果を大きく左右しない不明点は、既存実装、関連ドキュメント、慣例から判断して進める。結果に関係する仮定は最終報告に記載する。
- 質問する前に、関連ファイル、ログ、テストから回答を確認する。回答を必要としない独立した作業は先に進める。
- 確認が必要なのは、未承認の破壊的操作や重大な外部副作用、仕様・セキュリティ・費用を大きく変える判断、必要な権限や情報の不足、明確な作業範囲の拡大、またはユーザーが明示的に確認工程を要求している場合とする。
- 外部操作の承認を求める場合は、承認に依存しない準備と検証を先に済ませ、対象、変更内容、影響を確認できる状態で提示する。
- 作業中の追加指示は現在の作業に反映する。中止や目的変更が明示されない限り、元の完了条件も維持する。

## 指示の解釈

- 上位のシステム・開発者指示に従ったうえで、明示されたユーザー指示を skill やリポジトリ内のガイドラインより優先する。
- skill やルールの曖昧な記述から、追加承認が必要だと推測しない。適用条件と会話中の承認を確認する。
- skill やルールを理由に確認、停止、未完了、方針変更が必要になった場合は、該当ファイルへのリンク、根拠となる指示の引用、適用理由を簡潔に示す。明示的な要件とエージェント自身の解釈を区別する。

## 基本方針

- 常に日本語で回答する。
- 作業前に `git status --short` を確認し、既存の未コミット変更を把握する。
- ユーザーによる変更を勝手に戻さない。
- 変更前に関連する設計書、ルール、既存ファイルを確認する。
- plan を参照する前に `docs/plans/plan-status.md` を確認し、`completed` / `deprecated` の plan はユーザーが明示した場合を除き参照しない。
- plan の各 Step に着手する前に、Step ごとの独立した Codex タスク作成を完了する。作成に失敗した場合は、元のタスクで実装を開始せず、原因を確認して再試行する。前の Step と同じタスク内で次の Step を続行しない。詳細は `.codex/rules/plan-rule.md` に従う。
- plan の Step に着手するときは、先に `main` を最新化し、その最新 `main` から Step 用ブランチを作成する。
- Step に着手するときは GitHub Issue を作成し、作業完了時は対応 PR を作成する。
- plan 外の機能修正、バグ修正、ドキュメント修正、設定変更など、変更をリポジトリに入れる作業でも、原則として先に `main` を最新化し、GitHub Issue を作成して、Issue 番号を含むブランチで作業し、作業完了時は対応 PR を作成する。
- PR はユーザーが明示的に draft を指定した場合を除き、ready/open PR として作成する。作業途中、検証未完了、レビュー待ちなどを理由に draft を既定にしない。
- 画面修正を含む PR では、修正後の画面キャプチャを PR 本文に添付する。詳細は `.codex/rules/git-rule.md` に従う。
- 質問回答、調査のみ、変更を伴わない確認は Issue / PR 作成の対象外とする。
- PR 作成後はレビュー支援ツールのコメントを一定時間（少なくとも 5 分間）確認し、対応可能なレビューコメントがあれば対応・返信してから完了報告する。
- 仕様や設計に影響する変更では、関連ドキュメントの更新要否を確認する。

## 参照先

- プロジェクト全体の作業ルール: `.codex/rules/project-rule.md`
- アーキテクチャ境界・認可・runtime guard ルール: `.codex/rules/architecture-rule.md`
- Git / branch / PR / commit ルール: `.codex/rules/git-rule.md`
- format / lint ルール: `.codex/rules/format-rule.md`
- plan ルール: `.codex/rules/plan-rule.md`
- plan ステータス管理: `docs/plans/plan-status.md`
- 作業計画: `docs/plans/`
- システム設計: `docs/designs/system/`
- UI デザイン: `docs/designs/ui/ui-design.md`

## 作業時の注意

- ファイル検索は `rg` / `rg --files` を優先する。
- 手動編集は `apply_patch` を使う。
- 新規追加または契約を変更する公開 API、React component、hook、server action、route / use-case / repository entrypoint には、`.codex/rules/format-rule.md` に従って docstring / JSDoc を記載・更新する。
- 認可、SQL row 取得、app/package 境界、server action の責務に触れる場合は、`.codex/rules/architecture-rule.md` との整合を確認する。
- UI、画面遷移、レイアウト、コンポーネント方針を変更する場合は、`docs/designs/ui/ui-design.md` との整合を確認する。
- Google Workspace データ、個人情報、OAuth token、secret を扱う変更では、収集範囲、PII マスク、ログ出力、コスト影響を確認する。

## 検証と完了

- `.codex/rules/format-rule.md` に従い、変更の影響範囲に合う検証を実行する。必須の hook・CI チェックは維持する。
- テストは変更による振る舞いや回帰リスクを検証する。軽微で可逆的な変更に対し、実装をそのままなぞるだけのテストを追加しない。
- 必要な検証が成功した後は、新しい変更、失敗、未解決の懸念がある場合に限って検証範囲を広げるか再実行する。
- 完了前に差分を確認し、依頼範囲、既存変更の保護、関連ドキュメントの更新要否を確認する。
- 最終報告には、変更内容と理由、実行した検証コマンドと結果、未検証事項とその理由・残るリスクを記載する。CI / GitHub Checks を確認した場合は、対象 PR と確認時点の状態も記載する。実行していない検証や確認していないチェックを成功したものとして扱わない。

## コミュニケーションと委任

- 日本語で結論を先に述べ、必要な根拠を簡潔に説明する。箇条書きや表は、手順や比較を読み取りやすくする場合に使う。
- 進捗報告では、判明したこと、残る不確実性、次に確認することを伝える。
- サブエージェントの利用は、ユーザー指示と実行環境の制約に従う。利用する場合は、独立して進められる明確な調査・実装・検証を割り当て、親エージェントが結果を統合して確認する。
- サブエージェントへの委任を、plan の Step ごとに必要な独立した Codex タスク作成の代わりにしない。
